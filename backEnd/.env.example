# Backend Environment Variables (backend/.env)

# Flask Secret Key: Used for securing sessions and other security-related functions.
# Generate a strong, random string for production.
SECRET_KEY='your_flask_secret_key_here'

# Database URL: Connection string for your database.
# Examples:
# SQLite (file-based, good for development): sqlite:///site.db
# PostgreSQL: postgresql://user:password@host:port/database_name
# MySQL: mysql+mysqlconnector://user:password@host:port/database_name
DATABASE_URL='sqlite:///site.db'

# Add any other environment variables your Flask backend uses here.
# Example: API keys for external services
# EXTERNAL_API_KEY='your_external_api_key'

# Flask configuration variables (optional, can also be in config.py)
# FLASK_APP=app.py # Or your main Flask application file
# FLASK_ENV=development # Set to 'production' for production